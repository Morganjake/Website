<div id="title">Binary Lifting</div>
<p>
    Binary lifting a dynamic programming technique to find the Kth parent of a node in a tree in <i>O(log(n))</i> time.
    We can achieve this by pre-computing the ancestors of each node.
</p>
<p class="header">Step 1: Creating the ancestor table</p>
<p>
    First
</p>